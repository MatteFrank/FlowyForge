# Decisions

- Keep reusable code in `src/flowyforge/`.
- Keep fast-track experiments and external examples in `prototypes/`.
- Start with lightweight YAML configs and smoke tests before adding orchestration tools.

